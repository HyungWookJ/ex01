<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.LoginMapper">

	<select id="getList" resultType="org.zerock.domain.LoginVO">
		<![CDATA[
		select * from tbl_sign where bno > 0
		]]>
	</select>
	<insert id="insert">
		insert into tbl_sign(id, password, name)
		values(#{id}, #{password}, #{name})
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="int">
			select max(bno)+1 from tbl_sign; 
		</selectKey>
		
		insert into tbl_sign(bno, id, password, name)
		values(#{bno}, #{id}, #{password}, #{name});		
	</insert>
</mapper>